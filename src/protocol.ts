// Standalone, dependency-free subpath export (`@enonic/page-editor/protocol`)
// for hosts that only need the message catalog, the transport, and the page
// JSON model — without pulling in the editor runtime.

export * from './page-editor/protocol';
